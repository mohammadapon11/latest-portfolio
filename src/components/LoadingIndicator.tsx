'use client';

// Non-blocking loading indicator - preserves API compatibility without blocking initial page paint
const LoadingIndicator = () => {
  return null;
};

export default LoadingIndicator;

